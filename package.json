{
  "name": "dare-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npx vite build && cd .. && mkdir -p backend/public && cp -r frontend/dist/* backend/public/",
    "start": "cd backend && node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }

}
